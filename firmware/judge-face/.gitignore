# Local network config + WiFi credentials — never publish to the repo.
# Copy settings.toml.example to settings.toml and fill it in.
settings.toml
# Leftover from the retired Arduino sketch (was the same role: local creds).
secrets.h
